{
  "name": "lightplan-scripts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Automation scripts for testing the LightPlan viewer",
  "scripts": {
    "postinstall": "playwright install chromium",
    "test:preview": "node preview-regression.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
